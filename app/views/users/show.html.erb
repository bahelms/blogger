<%= provide(:title, @user.username) %>
<h1><%= @user.username %></h1>

<div class="content">
  <section id="profile" class="offset1">
    <%= render 'users/profile' %>
  </section>
  <section id="posts" class="offset1">
    <%= render 'users/recent_posts' %>
  </section>
</div>
  
