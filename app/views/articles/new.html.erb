<%= provide(:title, "Write a New Article") %>
<h1>Write a New Article</h1>

<div class="row">
  <div class="offset2">
    <%= form_for [@user, @article] do |f| %>
      <%= render 'articles/form_fields', f: f %>
      <%= f.submit "Publish", class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
