<header>
  <div class="container">
    <nav>
      <div class="navbar">
        <div class="navbar-inner">
          <ul class="nav"><li><%= link_to 'Home', root_path %></li></ul>
          <% if signed_in? %>
            <%= render 'layouts/signed_in_menu' %>
          <% else %>
            <ul class="nav"><li><%= link_to 'Sign In', signin_path %></li></ul>
          <% end %>
        </div>  
      </div>
    </nav>
  </div>
</header>